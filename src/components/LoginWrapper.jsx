import React, { useEffect, useState } from "react";
import Login from "./Login";
import { loginUser } from '../utils/utils';

const LoginWrapper = () => {
    //TODO if we couldn't use it till end, remove it and use the original component
    return <Login />
}